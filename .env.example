# Copy to .env for local testing (do NOT commit .env).
# On GitHub, set these as repository Secrets instead (Settings → Secrets and variables → Actions).
TELEGRAM_BOT_TOKEN=123456789:AAAA-your-bot-token-here
TELEGRAM_CHAT_ID=123456789
